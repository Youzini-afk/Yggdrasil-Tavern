const EXTENSIONS = [['token-counter', 'Token analysis and prompt chunk accounting', 'enabled'], ['regex', 'GLOBAL/PRESET/SCOPED text transforms', 'enabled'], ['quick-reply', 'Quick reply sets and slash triggers', 'enabled'], ['memory', 'Summary insertion and update plans', 'plan-only'], ['vectors', 'Retrieval index/query/injection plans', 'plan-only']] as const;
export function ExtensionsPanel(): JSX.Element { return <section className="drawer-panel product-extensions-panel"><h2>Extensions</h2><div className="panel-list">{EXTENSIONS.map(([id, body, status]) => <article key={id} className="panel-row panel-row-with-badge"><span><strong>{id}</strong><span>{body}</span></span><em>{status}</em></article>)}</div><div className="permission-card"><strong>Permission gate</strong><p>Third-party extension loading stays disabled until loader and host approval land.</p></div></section>; }
