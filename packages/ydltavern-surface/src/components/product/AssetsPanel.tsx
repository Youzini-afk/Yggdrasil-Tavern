const ASSETS = [['Character cards', 'V1/V2/V3 JSON and PNG metadata import/export'], ['World books', 'Lorebook entries, routing, activation diagnostics'], ['Presets', 'OpenAI/text/context/instruct/sysprompt shapes'], ['Chats', 'JSONL history import/export into Turn model']] as const;
export function AssetsPanel(): JSX.Element { return <section className="drawer-panel product-assets-panel"><h2>Assets</h2><div className="drop-zone">Drop ST assets here once host file access is wired.</div><div className="panel-list">{ASSETS.map(([title, body]) => <article key={title} className="panel-row"><strong>{title}</strong><span>{body}</span></article>)}</div></section>; }
