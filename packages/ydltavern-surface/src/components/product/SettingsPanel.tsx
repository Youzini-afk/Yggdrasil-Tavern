const SETTINGS = [['Connection', 'Provider profile, secret_ref, stream mode'], ['Sampler', 'Temperature, top_p, top_k, penalties, max tokens'], ['Persona', 'User persona, avatar, lock scope'], ['Theme', 'CSS variables, background, density'], ['Preset', 'Prompt order, instruct, sysprompt, context']] as const;
export function SettingsPanel(): JSX.Element { return <section className="drawer-panel product-settings-panel"><h2>Settings</h2><div className="panel-list">{SETTINGS.map(([title, body]) => <article key={title} className="panel-row"><strong>{title}</strong><span>{body}</span></article>)}</div></section>; }
